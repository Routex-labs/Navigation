# 운영(런타임) 의존성만 둔다 — Docker 이미지(Dockerfile)가 이 파일만 설치한다.
# 테스트 실행 도구(pytest·httpx)와 CI 게이트 도구(ruff·mypy·pip-audit)는
# requirements-ci.txt로, 노트북 분석 도구는 requirements-dev.txt로 분리한다.
#
# 재현성: 하한만 있으면 빌드 시점에 따라 다른 버전이 깔린다. 모든 항목에 상한을
# 둬(호환 릴리스 `==X.Y.*` 또는 `<major`) 같은 소스가 같은 버전을 받도록 한다.
# 정확한 lock(pip-compile --generate-hashes)은 후속 과제로 남긴다
# (docs/backend/refactor/08-dependency-model-supply-chain.md 참고).
fastapi==0.115.*
uvicorn[standard]==0.32.*
pydantic==2.9.*
pydantic-settings==2.14.*
sqlalchemy==2.0.*
numpy>=1.26,<3
mapbox-vector-tile>=2.2.0,<3.0.0
# 질의 형태소 정규화(경량 1차 매칭) — 순수 pip 휠, JVM 불필요. 설계: docs/backend/native/KIWI.md
kiwipiepy==0.23.*
# AI 자연어 질의(하이브리드 2차) — 문장 임베딩 + FAISS 의미 검색. 설계: docs/backend/native/FAISS.md
faiss-cpu==1.14.*
sentence-transformers==5.6.*  # torch를 전이 의존성으로 끌어온다(CPU 휠)
