# CI 게이트 전용 도구. 운영 이미지(requirements.txt)에는 넣지 않는다.
#   ruff       — lint + format 검사
#   mypy       — 타입 검사(app/ 대상, pyproject.toml 설정)
#   pip-audit  — 의존성 취약점 점검
#
# 설치: python -m pip install -r requirements-ci.txt
# 노트북 분석 도구는 requirements-dev.txt에 따로 있다.

ruff==0.16.*
mypy==2.3.*
pip-audit==2.10.*
