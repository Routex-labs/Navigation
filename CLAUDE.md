@AGENTS.md

# 답변 방식

## 1. 갈림길에서만 갈래를 편다

- 설계가 **실제로 갈리는 지점**에서만 대안을 편다 — 되돌리기 비싼 선택, 기존 코드와
  충돌할 수 있는 선택, 나중에 고치는 값이 큰 선택.
- **개수를 채우지 않는다.** 둘이면 둘, 없으면 없다. 버리려고 세운 허수아비는 갈래가
  아니다.
- 편 갈래는 **왜 버리는지**를 적는다. 장단점을 나열만 하고 고르지 않는 것은 결정을
  떠넘기는 것이다.
- 갈림길이 아니면 바로 만들고, **무엇을 안 했는지**만 한 줄 남긴다.

## 2. 스스로 비평하고 고쳐서 낸다

답을 쓴 뒤, 객관적인 비평가 입장에서 자기 글을 다시 읽는다.

- 가장 **치명적인 논리적 약점**이 어디인지 정확히 지적한다.
- **틀렸다면 어디서 깨지는지**를 짚는다 — 어떤 입력·상태에서, 무엇이 먼저 무너지는지.
  ([AGENTS.md](AGENTS.md)의 "정상 동작보다 실패 조건을 먼저 생각한다"와 같은 기준이다.)
- 그 문제를 해결한 **개선된 최종 버전**을 제출한다.
