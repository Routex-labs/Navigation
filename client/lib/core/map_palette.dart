/// 실내 도면 폴리곤의 색. 실내 화면([FloorPlanView])과 야외 지도의 실내 진입
/// 오버레이([indoor_overlay_layers.dart])가 **같은 도면을 그리므로** 두 곳이
/// 반드시 같은 값을 써야 한다. 예전에는 두 파일에 hex가 따로 박혀 있어, 한쪽만
/// 바꾸면 층을 오가는 사이에 도면 색이 바뀌어 보였다.
///
/// 규칙은 상용 지도와 같다 — **걸을 수 있는 곳은 밝게, 막힌 곳은 어둡게.**
/// 통로(footprint)가 가장 밝고 매장이 한 단계 어둡다. 실내 내비에서 사용자가
/// 가장 먼저 읽어야 하는 정보가 "어디로 걸을 수 있나"이기 때문이다.
///
/// 원래 값은 원본 SVG(`hyundai_floor_map_corrected_v6.svg`)에서 그대로 옮긴
/// `#F3F1EF`/`#D8D4D1`이었는데, 통로(`#FFFFFF`)와 명도 차가 2%도 안 나 통로와
/// 매장이 하나의 흰 덩어리로 보였다. **원본 SVG와는 더 이상 일치하지 않는다** —
/// 의도적으로 깬 것이다.
///
/// 대비를 fill로 만들지 않는다. 한때 매장을 `#EAE6E1`까지 어둡히자 구분은 됐지만
/// 도면 전체가 칙칙해졌다. 네이버 지도를 확대해 보면 매장 면은 흰 바닥과 거의 같은
/// 밝기이고, 매장끼리 공백도 없다. **구분은 전적으로 경계선이 한다.** 그래서 면은
/// 원본 근처에 두고 경계선의 명도만 내린다.
///
/// 색조는 **따뜻한 계열을 유지한다.** 네이버를 그대로 따라 차가운 라벤더 계열로
/// 옮겨 봤지만, 우리 베이스 지도(VWorld)는 도로가 베이지인 따뜻한 지도라 도면만
/// 따로 노는 색으로 보였다. 레퍼런스의 *방법*(면 대신 선으로 구분)은 가져오되
/// *색조*는 우리 베이스에 맞춘다.
library;

/// 건물 바닥. 걸어 다니는 공간이라 가장 밝게 둔다.
const mapFootprintFill = '#FFFFFF';
const mapFootprintOutline = '#00000088';

/// 매장 폴리곤 면. 원본(`#F3F1EF`)보다 한 톤만 낮다 — 면으로 구분하려는 게
/// 아니라 경계선이 얹힐 바탕을 주는 정도다.
const mapStoreFill = '#F1EEEA';

/// 매장 경계선. 도면에서 실제로 구분을 만드는 값이라 면과 명도를 크게 벌린다.
/// 면(약 94%)에 대해 60% 언저리다.
///
/// MapLibre의 `fillOutlineColor`는 **두께가 1px 고정**이다. 더 굵게 하려면 같은
/// 소스에 line 레이어를 따로 얹어야 하므로, 색으로 모자랄 때만 그 비용을 낸다.
const mapStoreOutline = '#A69C90';
