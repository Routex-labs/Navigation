/// 길찾기 "지도에서 선택"으로 **복도를 눌러 정한 지점**의 표시 이름.
///
/// 이름을 상수로 뽑아 둔 이유는 이 문자열이 세 화면에 흩어져 나타나기
/// 때문이다 — 실내 지도와 야외 실내 오버레이가 각각 후보를 만들고,
/// 상단 경로 초안 바(`MapTopBar`)와 길찾기 시트가 그 이름을 그대로 출력한다.
/// 각자 리터럴로 적어 두면 한쪽만 고쳤을 때 같은 지점이 화면마다 다른 이름으로
/// 보인다.
///
/// 매장 이름 자리에 들어가는 값이므로 **장소 이름처럼 읽히면 안 된다.**
/// "선택한 지점"처럼 일반명사로 두어, 사용자가 "내가 지도에서 찍은 곳"임을
/// 바로 알 수 있게 한다.
library;

const kMapPickedPointLabel = '지도에서 선택한 지점';
