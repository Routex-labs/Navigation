# 스타벅스 리저브 상세 화면 파일럿

작성일: 2026-07-30 · 최종 갱신: 2026-08-10 (소개 영상용 확장)

## 대상

- Studio id: `PO-HU40njvml1512`
- 표시명: 스타벅스 리저브
- 위치: 더현대 서울 B2

## 파일럿 범위

이 매장은 **소개 영상에 나오는 유일한 매장**이라, 상세 화면이 실제로 채워졌을 때 어떻게
보이는지를 검증하는 자리로 쓴다. 그래서 다른 매장보다 데이터가 두껍고, 영업시간처럼 다른
매장에는 넣지 않는 값도 여기에만 들어간다(설계 9-1 D2″).

현재 오버레이(`backend/resources/store_details/starbucks-thehyundai-seoul-b2.json`)에
들어간 것은 다음과 같다.

| 필드 | 내용 |
|---|---|
| `summary` | 한 줄 소개 |
| `hero` | 매장 사진 5장 |
| `menu` | 메뉴 30종 · 6개 카테고리 |
| `demoInfo` | 영업시간·대표번호·매장 타입·주차·위생등급 (확인일 2026-08-10) |
| `businessInfo` | 주소 |
| `links` | 공식 사이트·페이스북·인스타그램·스마트스토어 |

**가격은 넣지 않았다.** 아래 [메뉴](#메뉴)에 사유가 있다.

## 링크

| 라벨 | 주소 |
|---|---|
| 공식 사이트 | https://www.starbucks.co.kr/index.do |
| 페이스북 | https://www.facebook.com/starbuckskorea |
| 인스타그램 | https://www.instagram.com/starbuckskorea |
| 스마트스토어 | https://brand.naver.com/starbucks |

**넷 다 브랜드 채널이고 이 지점 전용이 아니다.** 지점 계정이 따로 있는지는 확인하지
않았다. 화면에서는 누르면 외부 브라우저로 열린다.

주소가 바뀌지 않을 것만 넣는다는 것이 이 필드의 조건이다. 검증기는 형식이 http(s)인지만
보고 **살아 있는지는 보지 않는다** — 시드가 외부 사이트 사정에 묶이면 남의 서버가 죽었을 때
우리 배포가 멈춘다.

## 이미지 출처

경로는 전부 `client/assets/place_details/` 이하다.

| 파일 | 용도 | 출처 | 라이선스 |
|---|---|---|---|
| `starbucks_store_0{1,4,6,7,8}.jpg` | hero 5장 (매장 사진은 이 5장만 남겼다) | 스타벅스 코리아 더현대서울(B2)R점 공식 매장 화면, 사용자 제공 | 데모용 |
| `starbucks_menu_*.jpg` 30장 | 메뉴 카드 | 스타벅스 코리아 공식 메뉴 페이지 (`image.istarbucks.co.kr`) | 상표·이미지: Starbucks Corporation, 데모 식별용 |

> **초기 파일럿 이미지 7장은 삭제했다.** `starbucks_logo.svg`,
> `starbucks_reserve_store_0{1..4}.png`, `starbucks_reserve_hero.jpg`,
> `starbucks_reserve_menu.jpg`는 위 사진들로 대체되어 어느 오버레이도 참조하지 않게 됐고,
> 경로 문자열만 쓰던 클라이언트 테스트를 현재 asset으로 옮긴 뒤 함께 지웠다. 매장 사진도
> 실제로 쓰는 5장만 남겼다.

> **메뉴 사진 원본 경로가 4장분만 남아 있다.** 초기 4종은 아래 [메뉴 사진 출처](#메뉴-사진-출처)에
> `skuimg` 경로가 적혀 있지만, 이번에 늘린 26장은 파일별 원본 URL을 받아 적지 않았다.
> 사진을 교체하거나 갱신하려면 공식 메뉴 페이지에서 다시 찾아야 한다.

## 확인된 매장 사실

- 더현대 서울의 공식 층별 안내는 B2 F&B에 스타벅스 리저브를 기재한다.
- 더현대 서울 공식 안내는 이 매장을 `스타벅스 더현대서울R점(B2)`으로 표기한다.
- 스타벅스 코리아는 리저브 매장을 스페셜티 커피와 전용 추출 방식을 경험하는 매장으로
  소개한다.
- 소개: 프리미엄 커피와 스페셜한 공간이 있는 더현대서울(B2)R점.
- 주소: 서울특별시 영등포구 여의대로 108 (여의도동, 파크원).
- 주차: 지원 불가.
- 오시는 길: 여의도역 3번 출구 지하 무빙워크를 통해 더현대 서울 B2와 연결.
- 인증: 식약처 음식점 위생등급제 매우우수 매장.

## 화면에 올린 항목과 뺀 항목

위 "확인된 매장 사실"을 전부 상세 화면에 올리지는 않는다.

| 항목 | 처리 | 사유 |
|---|---|---|
| 영업시간 | `demoInfo` | 화~목 10:30–20:00 · 금~일 10:30–20:30 |
| 대표번호 | `demoInfo` | `1522-3232`는 **이 매장 직통이 아니라** 전국 공통 고객센터다. 값에 그렇게 적어 둔다 |
| 매장 타입 | `demoInfo` | 리저브 매장이라는 사실이 이 매장을 고르는 이유다 |
| 주차 | `demoInfo` | 건물 단위 정보지만, 영상에서 "지원 불가"를 보여 줄 자리가 필요했다 |
| 위생등급 | `demoInfo` | 위와 같음 |
| 주소 | `businessInfo` | 건물 주소라 전 매장이 동일하다 — 뺄지 검토 중 |
| 오시는 길 | **제외** | 이미 실내에 들어와 있는 사용자에게 의미가 없다 |

**앞의 다섯 개는 원래 전부 "제외"였다.** 시간이 지나면 저절로 거짓이 되고 갱신을 보장할
방법이 없어서였고, 그래서 `forbidden_labels`가 `keyValue`·`businessInfo` 양쪽에서 조건 없이
막고 있었다. 소개 영상에 이 매장 상세가 나오면서 **이 한 매장에만** 예외를 열었고, 예외를
항목이 아니라 필드와 매장에 걸었다(설계 [9-1 D2″](place-detail-interface.md#9-1-갱신된-결정-파일럿-이후)).

- `demoInfo`는 `forbidden_labels`를 적용하지 않는 유일한 필드다.
- 그 대신 `_schema.json`의 `demo_allowlist`에 id(`PO-HU40njvml1512`)가 있는 매장만 쓸 수 있고,
  항목마다 `source`와 `confirmed_at`이 필수다.
- **다른 매장에 영업시간을 넣으려면 이 목록에 id를 더해야 하고, 더할 때는 그 매장을 실제로
  확인할 사람이 누구인지도 같이 정한다.**

`영업 중` 판정 문자열은 여전히 저장하지 않는다. 저장하는 순간 그 값은 저장 시점부터
틀리기 시작한다.

## 메뉴

메뉴 30종을 6개 카테고리로 넣었다. 카테고리는 화면의 탭이 되고, **탭 순서는 오버레이에
적힌 등장 순서**다(계약 4-2 규칙 3 — 순서는 서버가 정한다).

리저브 · 에스프레소 · 콜드 브루 · 프라푸치노 · 티·기타 · 푸드

값은 스타벅스 코리아 공식 사이트에서 옮겼다. 실제 목록은 오버레이 JSON이 단일 출처이고,
여기에는 **왜 그렇게 담겼는지**만 적는다.

### 가격을 넣지 않은 이유

초기 파일럿은 메뉴 4종에 가격을 붙였고, 출처가 외부 매장 정보 사이트
(`diningcode.com`)였다. 30종으로 늘리면서 **가격을 전부 뺐다.**

- 스타벅스 코리아 공식 사이트는 **가격을 공개하지 않는다.** 대신 용량·칼로리·카페인을 준다.
- 30종의 가격을 채우려면 출처가 다른 값을 섞거나 지어내야 하는데, 화면에서는 둘 다 똑같이
  "가격"으로 보인다. 틀린 가격 한 줄이 매장 소개 영상에 그대로 나간다.

그래서 스키마에서 `price`를 **선택 항목**으로 바꿨다. 가격이 없으면 카드가 그 자리에
용량·칼로리·카페인을 대신 보여 주고, 푸드처럼 영양정보도 없는 항목은 그 줄을 아예 그리지
않는다. 공식 가격 출처가 생기면 그때 `price`를 채우면 되고, 화면·스키마는 손댈 필요가 없다.

### 사진

30장 모두 스타벅스 코리아 공식 메뉴 페이지의 제품 사진이다(각 300×313). 초기 4종의 원본
경로만 기록에 남아 있다 — `https://image.istarbucks.co.kr/upload/store/skuimg/` 이하:

| 메뉴 | 원본 |
|---|---|
| 카페 아메리카노 | `2021/04/[94]_20210430103337006.jpg` |
| 카페 라떼 | `2021/04/[41]_20210415133833725.jpg` |
| 스타벅스 돌체 라떼 | `2021/04/[128692]_20210426091933665.jpg` |
| 카라멜 마키아또 | `2021/04/[126197]_20210415154609863.jpg` |
| 리저브 콜드 브루 | `2024/03/[9200000002093]_20240318144604476.jpg` |
| 리저브 나이트로 | `2021/02/[9200000002407]_20210225095106743.jpg` |

**원격 URL이 아니라 번들 asset으로 넣는 이유**는 설계 9-1 D1′의 조건이다 — 서버가 남의
이미지를 프록시하지 않고, 시트 첫 프레임이 네트워크를 기다리지 않는다(설계 8절 성능 기준).
URL을 그대로 `Image.network`에 넘기면 두 조건이 다 깨지고, CDN 경로가 바뀌면 화면이 빈다.

> 출처 페이지에는 `ⓒ 2026 Starbucks Coffee Company. All Rights Reserved.` 고지가 있다.
> 이 저장소의 다른 스타벅스 사진과 같은 **데모 식별용** 취급이며, 배포 전에 교체하거나
> 사용 허가를 받아야 한다.

이 외의 매장별 메뉴·가격·영업시간·서비스는 별도 공식 근거가 생긴 뒤 추가한다.
