# AI 자연어 질의 (경량 + FAISS 하이브리드) — 구현 설계

경량 텍스트 매칭([query.md](query.md))의 후속 단계. `query.md` 2절·9절에서 "후속"으로 남겨 둔
`② 형태소 정규화(Kiwi) → ③ 사전학습 임베딩(FAISS)` 을 검토하고 실제 구현 결정을 남긴 문서다.
구현 전에 **아키텍처·형태소기·벡터화 방식·실패 조건·검증 기준**을 먼저 확정한다.

> **구현 완료 상태.** 5절 3안 비교 후 **C(하이브리드)**로 확정·구현했다(11절). 임베딩 모델은
> `ko-sroberta-multitask`, 엔드포인트는 `POST /query/ai`, 임계값 0.50(실데이터 튜닝).
> 형태소(Kiwi)는 이번 범위에서 보류했고, 후속으로 **경량 1차 매칭에만** 적용했다 → [KIWI.md](KIWI.md).
> 임베딩 인덱스 텍스트·임계값은 그대로다(2차에는 질의 원문이 전달된다).

## 1. 해결할 문제

- 기존 경량 매칭은 **이름·카테고리·동의어의 문자열 일치**만 한다. 사전에 없는 표현은 못 잡는다.
  - "밥 먹을 데" → 음식점? 못 잡음(동의어 사전에 없음).
  - "애들 신발" → 아동 슈즈 매장? 못 잡음.
  - "화장실 급해" → FAISS 구현 당시 꼬리 제거 규칙 밖이었다. 후속 Kiwi 적용 뒤에는 1차에서 잡힌다.
- 동의어 사전을 무한정 늘리는 방식은 유지보수가 깨진다. **개념 단위 의미 검색**이 필요하다.
- 완료 조건(Definition of Done):
  - 사용자가 **AI 쿼리 버튼**으로 자연어를 넣으면, 사전에 없는 표현도 **의미가 가장 가까운 매장 1건**과 그 **입구 노드**를 돌려준다(온디바이스 경로용).
  - 경량 매칭의 응답 계약(`dto/query.py`)을 깨지 않는다 — 클라이언트 파싱 재사용.
  - 실패 조건(임계값 미달·건물 없음·입구 노드 없음)이 명시된 상태로 처리된다.

## 2. 범위

- **1차 목표 — 매장 안내.** 자연어 질의 → 매장 1건 + `entrance_node_id`. 경로 계산은 여전히 클라이언트(Dijkstra).
- **이번 범위 — AI 질의 경로 신설.** `POST /query/ai`에서 기존 경량 매칭을 1차로 실행하고,
  놓쳤거나 서로 다른 이름이 여럿 걸린 부분 일치의 원문을 FAISS 의미 검색 2차로 넘긴다.
  두 단계는 기존 응답 계약을 공유한다.
- **범위 밖 — 상품 광고·연관 매장 추천.** "이 근처 세일하는 곳", "MLB랑 비슷한 매장" 같은 2차 기능은 후속. 지금은 이음새(임베딩 인덱스)만 만들어 두고 구현은 미룬다.
- **범위 밖 — 경로 계산.** 서버는 도착 노드까지만. 최단 경로는 클라이언트.
- **범위 밖 — 대화형 LLM·생성 응답.** 이번 건 **검색(retrieval)** 이지 생성이 아니다. 문장 생성·요약 없음.
- **1차 대상 데이터 — B2 매장.** `query.md`와 동일하게 지하 2층 데이터로 먼저 구현·검증하고, 나머지 층은 같은 파이프라인으로 커버된다.

## 3. 아키텍처 — 두 경로의 공존

```
[상단 일반 검색]  ──▶  경량 매칭(_rank, Kiwi 정규화·동의어) ── 기존 엔드포인트
   빠름·정확 이름/동의어

[AI 쿼리 버튼]   ──▶  같은 경량 1차 ── 미스·모호한 tier 2 ──▶ 질의 원문 FAISS 2차
   자연어·개념                                      │
                                                    ▼
                                둘 다 QueryMatch(같은 응답 계약)로 수렴
```

- **엔드포인트**: `POST /query/ai`로 확정·구현했다. 응답은 `DestinationResponse`를 그대로 사용해 클라이언트 파싱을 재사용한다.
- **경량 매칭을 지우지 않는 이유**: 정확한 브랜드명("MLB")은 임베딩 유사도보다 문자열 일치가 더 정확·빠르고 오매칭이 없다. 상단 검색의 즉답성은 경량이 담당하고, AI 버튼은 "말로 푸는" 질의를 담당한다. 역할 분리.

## 4. 형태소 분석기 — 초기 검토와 후속 적용

초기안에서는 질의와 인덱스 문서 양쪽에 Kiwi를 적용하려 했다. 실제 FAISS 구현에서는 문장
임베딩에 원문을 유지했고, Kiwi는 후속 작업에서 **경량 1차 질의 정규화에만** 적용했다.
구현·전수 측정은 [KIWI.md](KIWI.md)에 있다.

- **현재 역할**:
  1. 경량 1차 질의 정규화 — 조사·어미·의문형 제거.
  2. 사용자 사전 — 동의어와 659개 고유 매장명을 등록해 브랜드 오분해 방지.
- **FAISS에 적용하지 않은 이유**: 문장 임베딩은 질의 원문을 그대로 처리하고, 인덱스 문서나
  질의 전처리를 바꾸면 실데이터로 정한 임계값 0.50을 다시 튜닝해야 한다.
- **선택 근거**:
  - 순수 pip 휠 설치, **JVM 불필요** — Windows·Docker·macOS 셋업이 동일하고 가볍다.
  - C++ 기반이라 빠르고, 신조어·띄어쓰기 교정에 강하다(브랜드명 다수인 이 데이터에 유리).
  - `query.md` 2절이 이미 "형태소 정규화(Kiwi 등)"로 지목 — 문서 일관성.

### 미선택 — 이유만 기록

- **KoNLPy(Okt/Komoran) / Mecab** — 성숙하지만 **JVM(jpype) 또는 네이티브 C 빌드 + 사전(mecab-ko-dic)** 이 필요하다. Windows·Docker 빌드 단계가 늘고, 개발자가 양 OS에 있는 이 프로젝트에선 셋업 마찰이 크다. Kiwi로 충분해 채택하지 않는다.

## 5. 벡터화 3안 비교 — 결정 기록

FAISS 인덱스에 넣을 벡터를 무엇으로 만들지가 의존성·이미지 크기·"진짜 의미검색" 수준을 정한다.
코퍼스 규모는 건물당 매장 수백~수천 건으로 작다(→ FAISS 인덱스는 어느 안이든 `IndexFlat` 브루트포스로 충분, IVF/HNSW 불필요).

| 항목 | A. 문장 임베딩(SBERT) | B. 형태소 TF-IDF + FAISS | C. 하이브리드(A+경량) |
|---|---|---|---|
| **핵심** | 한국어 SBERT로 문장을 밀집 벡터화 → 코사인 유사도 | Kiwi 토큰 → TF-IDF, LSA로 밀집화 → FAISS | 경량/형태소로 1차 정확·동의어, 미달 시 A로 2차 |
| **의미 이해** | ★★★ "밥 먹을 곳"→음식점 등 개념 매칭 | ★ 같은 형태소 공유해야 매칭(개념 X) | ★★★ (A와 동일) + 정확매칭 보정 |
| **신규 의존성** | `sentence-transformers`(+torch), `faiss-cpu` | `scikit-learn`, `faiss-cpu` | A와 동일 |
| **이미지/디스크** | 무거움(torch ~수백MB, 모델 120~500MB) | 가벼움(torch 없음, 수십MB) | 무거움(A와 동일) |
| **질의 지연(CPU)** | 임베딩 ~10~50ms + 검색 | ~수ms | 1차 히트면 ~수ms, 2차만 임베딩 |
| **콜드스타트** | 모델 로드 수 초. 이미지에 모델을 굽고 기동 시 백그라운드 워밍으로 흡수([배포 문서](../../guide/gcp-instance.md)) | 거의 없음 | A와 동일(2차 경로 로드) |
| **오매칭 위험** | 정확한 이름도 유사도에 맡겨 흔들릴 수 있음 | 낮음(어휘 일치 기반) | 낮음(정확 이름은 1차가 확정) |
| **구현·문서 복잡도** | 중 | 중(LSA 차원축소 단계) | 높음(두 경로 관리) |
| **"진짜 자연어" 부합** | 높음 | 낮음(사실상 나은 어휘검색) | 가장 높음 |

- **추천: C(하이브리드).** 이유:
  - 정확한 브랜드명은 **이미 만들어 둔 경량 매칭**이 확정하므로(오매칭 0), 임베딩은 "말로 푸는" 질의만 담당 → A 단독의 이름 오매칭 위험이 사라진다.
  - 추가 비용은 A 대비 크지 않다. 1차 경로가 이미 존재(`query_search._rank`)하므로, 그 뒤에 임베딩 fallback만 붙이면 된다.
  - 3절 아키텍처(경량=상단검색, AI=버튼)와 자연스럽게 맞는다.
- **차선: A(문장 임베딩 단독).** AI 버튼 경로를 처음부터 의미검색으로 단순하게 갈 때. 이름 오매칭은 임계값·정확일치 가드로 보완.
- **비추천 상황의 B.** "진짜 자연어"라는 이번 목표엔 개념 이해가 약해 부족하다. torch를 절대 넣기 싫은 제약이 우선일 때만.

> 모델 비교 당시 후보는 `intfloat/multilingual-e5-small`(384d)과
> `jhgan/ko-sroberta-multitask`(768d)였다. 현재 구현은 한국어 특화 후자로 확정했다.

## 6. 인덱싱 파이프라인 (현재 C)

- **인덱스 문서 단위**: 매장 1건 = `name + category + subcategory` 원문으로 만든 텍스트.
- **빌드 시점**: 모델은 이미지 빌드 때 캐시에 굽고(`scripts.warm_embedding_model`), 서버
  기동 직후 백그라운드 워밍 스레드가 로드한다(`NAV_WARM_EMBEDDING=1`, [배포 문서](../../guide/gcp-instance.md)).
  건물별 인덱스는 여전히 첫 의미 검색 때 1회 빌드해 메모리에 둔다. 워밍이 켜져 있어도 서버
  기동과 경량 검색은 모델과 독립이며, 인덱스 파일 영속화는 후속이다.
- **정규화**: 코사인 유사도 → 임베딩 L2 정규화 후 `IndexFlatIP`.
- **매핑**: FAISS 벡터 행 → `store.id` 역참조 테이블 유지(검색 결과를 `Store`로 되돌린다).

## 7. 질의 처리 흐름 (C 기준)

1. 요청 `{ text, building_id, current_floor_id? }` 수신.
2. **1차**: 경량 `_rank`가 구두점 후보·꼬리 제거·Kiwi 정규화 후 이름/동의어/카테고리를 매칭한다.
3. 정확 이름·카테고리와 최상위 매장명이 하나뿐인 부분 일치는 1차에서 확정한다.
4. **2차**: 1차 미스 또는 서로 다른 이름이 여럿 걸린 tier 2 부분 일치면, 정규화 전
   질의 원문으로 건물 FAISS 인덱스를 검색해 최상위 1건을 고른다.
5. `current_floor_id`가 있으면 해당 층으로 후보를 한정(경량·FAISS 양쪽).
6. `QueryMatch` 조립(입구 노드·좌표·층명) 후 `DestinationResponse`로 반환.

## 8. 실패 조건 (정상 동작보다 먼저 정의)

| 상황 | 처리 |
|---|---|
| `building_id`가 없는 건물 | `404` (기존 계약과 동일하게 라우터가 번역) |
| 1차·2차 모두 미스 | `200` + `{"status":"no_match","query":…,"match":null}` (에러 아님) |
| FAISS 최상위 유사도가 **임계값 미달** | `no_match`로 처리(엉뚱한 매장 반환 금지). 임계값은 실데이터로 튜닝하고 문서에 기록. |
| 매칭됐으나 `entrance_node_id`가 `null` | `match`는 주되 `status:"ok_no_route"`로 구분(경로 불가 인지). |
| `text`가 빈 문자열·공백뿐이거나 200자 초과 | `422`. 세 `/query` 엔드포인트가 같은 검증 타입을 쓴다. |
| 이름 부분 일치가 서로 다른 매장을 여러 개 잡음 | AI 경로는 ID순 첫 매장을 임의 확정하지 않고 FAISS로 넘긴다. 2차도 실패하면 `no_match`. |
| 임베딩 모델 로드 실패 | 서버 기동과 경량 검색은 유지한다. AI 요청의 1차가 놓친 질의는 `no_match`로 내려간다. 백그라운드 워밍 스레드가 실패해도 같은 방식으로 degrade한다. |
| 새 인스턴스의 첫 의미 검색 | 모델은 이미지에 구워져 있어 런타임 다운로드가 없고, 캐시 미스일 때만 Hub로 폴백한다(`_load_model`). 남는 비용은 모델 로드(약 6초)와 인덱스 1회 빌드이며, 워밍이 이를 기동 직후로 앞당긴다. |
| 동점·비결정성 | 경량은 `(tier, 후보 순서, level, store.id)`로 결정적이다. FAISS 단독의 동일 점수·동일 문서 층 순서는 명시적으로 재정렬하지 않으므로, 시설 안내는 `current_floor_id`를 전달해야 한다. |

## 9. 응답 계약 (기존 유지)

- `dto/query.py`의 `QueryMatch` / `DestinationResponse`를 **그대로 사용**한다. 새 필드 없이도 AI 경로가 매장 안내를 만족한다.
- 유사도 점수를 클라이언트에 노출할지는 후속 결정(디버그용으로만 로그에 남기고 계약엔 넣지 않는 편을 기본으로).

## 10. 검증 기준 (수용 기준)

구현이 아래를 만족하면 완료로 본다. 테스트는 `backend/tests`의 Given/When/Then·fixture 관례를 따른다.
**(구현 완료. 현재 전체 백엔드 회귀 테스트 수치는 [KIWI.md](KIWI.md) 6절에 기록하고,
모델이 필요한 임베딩 스모크 2개는 `NAV_TEST_EMBEDDING=1`로 별도 실행한다.)**

- [x] 사전에 **없는 자연어**("밥 먹을 곳"→요즘김밥, "화장품 사려고"→이솝·샤넬뷰티)가 의미상 맞는 매장을 반환한다. — 구현 당시 실데이터 라이브 확인. 현재 자동 스모크는 정확한 매장명까지 고정하지 않아 배포 전 수동 재확인 필요.
- [x] 정확한 브랜드명("가게A")은 **1차 경량 경로**로 확정된다(임베딩 미호출). — 단위 `test_1차_경량이_맞으면_임베딩을_호출하지_않는다`
- [x] 유사도 임계값 미달 질의가 `no_match`를 반환한다(엉뚱한 매장 금지). — 스모크 `test_무의미_질의는_임계값에서_걸러진다`
- [x] `current_floor_id`는 **1차 경량에만** 적용된다. 2차 의미 검색은 건물 전체를 본다. — 단위 `test_현재층에_없는_대상도_2차가_찾은_타층_매장을_반환한다`, 스모크 `test_현재층에_업종이_없어도_AI질의는_타층에서_찾는다`
  - 초기 구현은 2차에도 층 필터를 걸었는데, 1F에서 "밥집"을 물으면 상위 후보가 전부 B1 식당가라 필터에 전멸해 항상 `no_match`였다. 자연어 질의를 던지는 사용자는 대상 층을 모르고, 응답의 `floor_name`이 층까지 안내하므로 2차의 층 스코프를 걷어냈다. 현재 층 우선이 필요한 시설 찾기("화장실")는 1차가 층 스코프를 유지한 채 확정한다.
- [x] `entrance_node_id` 없는 매장이 매칭되면 `ok_no_route`로 구분된다. — 기존 `_status` 재사용
- [x] 없는 `building_id`가 `404`를 반환한다. — 통합 `test_ai_질의_없는_건물은_404를_반환한다`
- [x] 응답 스키마가 기존 `DestinationResponse`로 고정된다. — 라우터 `response_model=`
- [x] 모델 로드 실패 시에도 경량 검색은 계속 동작한다. — 단위 `test_모델_없어도_semantic_search는_None으로_degrade`
- [x] 모호한 부분 일치는 FAISS에 원문을 넘기고(현재 층은 넘기지 않는다), 단일 부분 일치는 1차에서 끝난다. — `test_query_ai.py`
- [x] 최종 하이브리드 경로를 긍정 25 + 부정 4, 총 29개로 재평가했다. — 기대 패턴 23/29,
  FAISS 단독 22/29, 부정 4건 모두 `no_match`. 재현: `backend/scripts/evaluate_query_hybrid.py`
- [x] `backend/tests` 단위/통합 테스트 통과. — 최신 전체 수치는 [KIWI.md](KIWI.md) 6절

> 참고: 클라이언트 연동(**AI 쿼리 버튼** UI, `/query/ai` 호출·파싱, 결과 지도 표시)은 별도 클라이언트 작업.

## 11. 확정 / 미결

- **확정 — 벡터화(5절)**: **C. 하이브리드**. 1차 경량 `_rank`(정확·동의어) → 2차 FAISS 임베딩.
- **확정 — 임베딩 모델**: `jhgan/ko-sroberta-multitask`(768차원). 한국어 의미 품질 확인("화장품"→뷰티 0.70).
- **확정 — 엔드포인트**: `POST /query/ai` 신설. 응답은 `DestinationResponse` 재사용.
- **확정 — 유사도 임계값**: `SIMILARITY_THRESHOLD = 0.50`. 현재 평가 세트는 thehyundai-seoul 실데이터
  매장 1531건, 긍정 25 + 부정 4 = 질의 29개다. 근거는 아래 11-1.
- **확정 — 역할 분리**: 경량 매칭 = 일반 검색과 AI의 확실한 1차, FAISS = AI 경로의 미스·모호한 부분 일치 2차.
- **확정 — 1차 목표**: 매장 안내(매장 1건 + 입구 노드). 상품 광고·연관 매장은 후속.
- **후속 완료 — 형태소기(Kiwi)**: 경량 1차에만 적용했다. FAISS 2차의 질의·인덱스 텍스트는 원문을 유지한다.
- **후속 완료 — 최종 AI 경로 재평가**: 29개를 `match_ai_destination()`으로 실행했다.
  Kiwi로 1차가 잡은 질의는 5개였고, 그중 다중 tier 2 `"커피 마시고 싶어"`만 FAISS로
  넘기도록 수정했다. 최종 기대 패턴 23/29로 FAISS 단독 22/29보다 1건 높다.
- **후속 완료 — 임베딩 모델 준비**: 모델을 이미지에 굽고(`scripts.warm_embedding_model`) 로컬
  캐시 우선 로드(`_load_model`)로 런타임 다운로드를 없앴으며, 기동 직후 백그라운드 워밍으로
  첫 질의 지연을 흡수한다. 배포 스펙은 [gcp-instance.md](../../guide/gcp-instance.md).
- **미결 — 인덱스 영속화**: 건물별 FAISS 인덱스는 아직 첫 의미 검색 때 빌드해 메모리에만 둔다.
  다건물·다인스턴스 확장 시 준비된 인덱스 아티팩트를 읽어 콜드 경로를 더 줄인다.
- **미결 — FAISS 단독 동점 층 순서**: 동일 이름 시설이 여러 층에 있으면 FAISS 반환 순서를
  명시적으로 재정렬하지 않는다. 최종 AI 경로는 시설 exact를 경량에서 결정적으로 처리하지만,
  사용자의 현재 층 안내에는 `current_floor_id`를 보내는 것을 계약으로 유지한다.

최종 경로 평가는 로컬 DB를 시드한 뒤 모델 의존성이 있는 명령으로 재현한다.

```powershell
Set-Location backend
python -m scripts.seed.reset_and_seed
python -m scripts.evaluate_query_hybrid
```

이 스크립트의 기대 패턴 판정은 이름·카테고리·서브카테고리의 최소 자동 지표다. 예를 들어
`운동화 사고 싶다 → 나이키 라이즈`처럼 사람이 보기에는 타당해도 패턴에 없는 결과는 실패로 센다.
따라서 `23/29`는 절대 품질 점수가 아니라 같은 데이터에서 경로 변경 전후를 비교하는 회귀 지표다.

### 11-1. 임계값 0.50을 더 낮추지 않은 이유 — 마진이 0.006뿐

thehyundai-seoul 29개 질의 평가에서, **0.50 미만이라 놓치지만 사실 맞는** 질의가 둘 있다.
`에스컬레이터`도 FAISS 단독은 실패하지만 현재 Store 데이터의 경량 정확 일치가 최종 경로에서 처리한다.

| 질의 | top 점수 | 최상위 후보 | 문제 |
|---|---|---|---|
| `현금 뽑을 데` | 0.464 | ATM (하나은행) | ATM이 top인데 미달로 놓침 |
| `가방 보러 왔어` | 0.453 | 타임파리(잡화·액세서리) | 관련 매장이나 미달 |
| `에스컬레이터` | 0.447 | 리모와(명품) — 엉뚱 | Store는 12개 층 43건 존재. FAISS 문장 의미가 못 잡지만 최종 경량 tier 0이 처리 |

그런데 무의미 질의 점수가 바로 아래 붙어 있다: `asdfqwerzxcv` **0.458**, `ㅋㅋㅋㅋㅋ` 0.437.

**유효한 ATM(0.464)과 무의미(0.458)의 간격이 0.006뿐이다.** 임계값을 0.46으로 낮추면
ATM은 잡지만 `asdfqwerzxcv`도 통과해 엉뚱한 매장을 답으로 내보낸다. 길찾기에서는
"틀린 매장 안내" > "다시 말해 주세요" 가 더 나쁘므로, 0.50은 **정밀도 우선** 선택이다.

즉 이 값은 재현율을 몰라서 높게 잡은 게 아니라, 위 마진 때문에 **절대 임계값 하나로는
더 못 내리는** 것이다. 재현율을 올리려면 임계값이 아니라 다음을 손대야 한다.

1. **임베딩 텍스트 보강** — 매장에 설명·동의어를 붙여 임베딩(예: ATM → "ATM 현금 인출 은행"). 이름만으로는 신호가 약하다.
2. **형태소(Kiwi) 정규화** — 경량 1차에는 후속 적용 완료. FAISS 점수에 적용하려면 임계값 재튜닝이 필요하다.
3. **시설·서비스 텍스트 보강** — ATM·물품 보관처럼 이름만으로 약한 항목에 설명·동의어를 넣되,
   Store와 POI 이중 표현은 역할을 명확히 한다.
4. **마진/상대 기준** — 절대 임계값 대신 top1-top2 간격 등으로 무의미 질의를 거른다.

> 질의별 상위 3건 점수 나열은 [`backend/notebooks/faiss_eval.ipynb`](../../../backend/notebooks/faiss_eval.ipynb)에서 재생성한다.

## 12. 향후 확장 (지금 구현 안 함)

- **연관 매장·상품 추천** — 이번에 만드는 임베딩 인덱스를 재사용해 "비슷한 매장", "이 근처 세일" 등으로 확장.
- **인덱스 영속화** — 모델은 이미지에 구워 콜드 경로를 줄였다(11절). 남은 건 인덱스로, 첫 의미
  검색의 빌드 대신 준비된 아티팩트를 읽어 다인스턴스 콜드 경로를 더 단축.
- **다건물 확장** — 건물별 인덱스 관리 전략(현재는 건물당 in-memory).
- **유사도 점수 노출** — 클라이언트 UX 필요 시 계약에 추가.
